const commando = require( 'discord.js-commando' );

class ChatWithMe extends commando.Command {
  constructor( client ) {
    super( client, {
      name: 'chat',
      group: 'random',
      memberName: 'chat',
      description: 'A few basic responses.'
    } );
  }
  
  async run( message, args ) {
    
  }  
}

module.exports = ChatWithMe;